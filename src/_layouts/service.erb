---
layout: default
---

<div class="content full-width">
  <div class="intro">
    <h1 class="title"><%= resource.data.title %></h1>
    <div class="buttons">
      <% resource.data.tags.each do |tag| %>
        <span class="button"><%= tag %></span>
      <% end %>
    </div>
  </div>
  <div class="graphic">
    <%= svg "/images/#{ resource.data.image }" %>
  </div>
  <div class="description">
    <%= yield %>
  </div>
</div>
<aside class="aside">
  <nav class="puffy nav">
    <span class="desktop">Select</span>
    <ul class="links">
      <% collections.services.resources.each do |service|  %>
        <li class="link <%= resource == service ? 'active': '' %>"><a class="icon" href="<%= service.relative_url %>"><%= svg "/images/#{ service.data.icon }" %></a></li>
      <% end %>
    </ul>
  </nav>
  <a class="puffy desktop">View our <%= resource.data.title %> projects </a>    
</aside>
