<gallery class="full-width" data-controller="image-row-sizer">

  <% @srcs.each do |src| %>
    <%= render Shared::Image.new(src: "#{@resource.data.image_directory}/#{src}") %>
  <% end %>
  
</gallery>